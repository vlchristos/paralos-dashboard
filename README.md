# Paralos assignment

Run locally in development mode

```bash
npm install
npm run dev
```

Build & run locally

```bash
 npm install
 npm run build
 npm run preview
```

Main tools and frameworks used:

- React
- Redux toolkit
- Material UI
